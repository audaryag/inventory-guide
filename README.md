# Inventory Power BI — build guide

Live page: https://audaryag.github.io/inventory-guide/

`BUILD_GUIDE.md` is the source of truth. `build.py` regenerates `index.html` from it
(the page's Copy buttons and progress ticks are generated, not hand-written).
